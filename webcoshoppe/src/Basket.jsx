import React from 'react';

// props is what you feed into it //
const Basket = props => {
    return (<div> You're basket has {props.size} items and total is ${props.price}. </div>)

}







export default Basket